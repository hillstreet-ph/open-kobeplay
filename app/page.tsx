const cards=[["Client balance","Credits and card funding"],["Invoices","Recurring charges and reminders"],["Approved costs","Tools, subscriptions and staffing"],["Audit trail","Immutable payment and approval events"]];
export default function Home(){return <main className="shell"><header className="top"><b>OPEN PAYMENT</b><span className="badge">Private beta</span></header><h1>One secure gateway for client billing and internal operations.</h1><p className="lead">Clients see approved totals, invoices, credits and payment status. Internal payroll, supplier costs and KobePlay margin remain private.</p><section className="grid">{cards.map(([a,b])=><article className="card" key={a}><strong>{a}</strong><span>{b}</span></article>)}</section><aside className="notice">Access is tenant-scoped and denied by default. The client portal never exposes AITeam, Private Operations, secret values or internal cost calculations.</aside></main>}